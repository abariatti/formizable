import { NgModule } from '@angular/core';
import { FormizableComponent } from './formizable.component';

@NgModule({
  imports: [
  ],
  declarations: [FormizableComponent],
  exports: [FormizableComponent]
})
export class FormizableModule { }
